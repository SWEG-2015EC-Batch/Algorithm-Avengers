 //calculating miles a car can be driven
#include<iostream.h>
int main()
{
    int capacity, miles_per_gallon, miles_drivable;
    cout<<" Enter capacity in gallon the tank can carry ";
    cin>>capacity;
    cout<< " Enter miles per gallon the automoblie can be driven ";
    cin>>miles_per_gallon;
    miles_drivable = capacity * miles_per_gallon;
    cout<<" miles it can be driven = "<<miles_drivable<< " miles without refuling ";
    return 0;
}
